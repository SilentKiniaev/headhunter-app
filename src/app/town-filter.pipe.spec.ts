import { TownFilterPipe } from './town-filter.pipe';

describe('TownFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TownFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
